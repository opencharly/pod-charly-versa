# 2026.265.1900 — refactor: drop the moved family skill from pod-charly-versa

## Summary

Remove the 1 skill entity from this repo whose artefact has moved to its owning repo. It now lives as a `skill:` entity in the distro-* owner repos (the producer PRs merged and tagged them) and is generated into the marketplace from there. Keeping a second copy here is an R3 one-owner violation.

**Files in the diff** (both accounted for):

- `charly.yml` — deletes the entity `versa` and bumps the schema stamp (`version:`) to the current toolchain revision.
- `CHANGELOG/2026.265.1657.md` — this repo's per-release history file (standing convention).

## How tested

Change class: candy/box config (skill-entity deletion) → the config-class gate is
`charly box validate` over this repo's tree.

**1. `charly box validate` on this branch (post-deletion), with a NEGATIVE CONTROL
proving it genuinely loads/validates `skill:` entities** — breaking the `name:`
field of a REMAINING skill entity in this same tree makes validate FAIL, naming
that node:

```
$ charly box validate
charly box validate: OK — checked 1 candy, 7 distros, 7 builders; 0 warnings, 0 errors

$ # negative control: delete `name:` from the remaining `airflow-layer-skill`
$ charly box validate
charly: error: command "validate": validation error: …/charly.yml:doc0: node "airflow-layer-skill":
  plugin kind:skill: plugin_input fails #SkillInput: #SkillInput.name: incomplete value =~"^[a-z][a-z0-9-]*$"
```

- **Disposable target:** N/A — config class; no image/deploy is created or changed.
- **Fresh-rebuild confirmation:** N/A — config class; the repo builds no binary
  (`charly box validate` is the class gate).
- **Did the changed path execute live?** Yes — the deleted entities' successor
  path (the marketplace generation closure) ran; see proof 4.

**2. Successor liveness — each deleted entity is present on its owner's pinned tag
(real, executed output, line numbers included):**

```
$ git -C distro-cachyos show v2026.265.1729:candy/cachyos-app-skills/charly.yml | grep -n 'name: versa$'
474:        name: versa
```

**3. Repo-wide sweep (UNNARROWED `grep -rn`, all files, not just `charly.yml`), with
each hit reconciled** — the deleted entity DEFINITIONS are gone; the only hits are
(a) this PR's own CHANGELOG entry naming what it deleted, and (b) prose
cross-references like `/charly-jupyter:jupyter-ml-notebook` or `/charly-selkies:selkies-labwc`,
which name a skill that still EXISTS (now owned by the distro repo) — those are
read and kept, not mechanically deleted:

```
./CHANGELOG/2026.265.1657.md:1:# 2026.265.1657 — refactor: drop the moved family skill from pod-charly-versa
./CHANGELOG/2026.265.1657.md:9:- `versa-skill:`
./charly.yml:2:charly-versa:
./charly.yml:6:            Metadata concept candy owning the charly-versa skills whose names have no namesake candy (versa geo/marimo images). Owns its family's `skill:` kind entities (sibling nodes in this file) — the
./charly.yml:14:            - check: the versa concept candy's skill entities are present and generated into the marketplace's versa/skills
./charly.yml:15:              id: charly-versa-present
./charly.yml:23:        family: versa
./charly.yml:24:        owner: charly-versa
./charly.yml:26:            Apache Airflow 3.x with LocalExecutor + SQLite (single-node, dev-friendly), 4 supervisord services (init, scheduler, dag-processor, webserver). Layer is service-only — its Python deps live in /charly-versa:marimo-layer's pixi env. No MCP wrapper (no upstream v2 release exists; consumers drive Airflow via direct REST /api/v2 calls).
./charly.yml:37:            deps (`apache-airflow`) live in `/charly-versa:marimo-layer`'s pixi env,
./charly.yml:167:            The marimo notebook in `/charly-versa:notebook-osm` writes its own DAG
./charly.yml:183:            - `/charly-versa:marimo-layer` — pixi env that owns the airflow Python deps
./charly.yml:184:            - `/charly-versa:notebook-osm` — canonical user of the REST trigger pattern
./charly.yml:185:            - `/charly-versa:versa` — the image composing this layer
./charly.yml:192:        family: versa
./charly.yml:193:        owner: charly-versa
./charly.yml:202:            helpers. Composed by `/charly-versa:versa` so operators can debug
./charly.yml:206:            Without this layer, `charly cmd versa "ps"` returns
./charly.yml:296:            ## Use case: debugging versa services
./charly.yml:309:            - `/charly-versa:versa` — image composing this layer
./charly.yml:310:            - `/charly-versa:airflow-layer` — services this toolkit debugs
./charly.yml:311:            - `/charly-versa:osm-tools-layer` — martin server this toolkit debugs
./charly.yml:312:            - `/charly-versa:marimo-layer` — marimo runtime this toolkit debugs
./charly.yml:313:            - `/charly-coder:dev-tools` — overlapping but heavier (includes qemu, AWS CLI, etc.); not composed by versa
./charly.yml:318:        family: versa
./charly.yml:319:        owner: charly-versa
./charly.yml:329:            `http.server`. Composed by `/charly-versa:versa` so operators can
./charly.yml:331:            server (`/charly-versa:osm-tools-layer`).
./charly.yml:427:            ## Use case in versa
./charly.yml:429:            The streets-map style in `/charly-versa:notebook-osm` cell 7 is a
./charly.yml:441:            - `/charly-versa:versa` — image composing this layer
./charly.yml:442:            - `/charly-versa:osm-tools-layer` — companion martin tile server
./charly.yml:443:            - `/charly-versa:notebook-osm` — uses an inline style; maputnik can author richer ones
./charly.yml:449:        family: versa
./charly.yml:450:        owner: charly-versa
./charly.yml:546:            `/charly-versa:notebook-osm` to render martin's vector tiles
./charly.yml:551:            bypass is documented in `/charly-versa:notebook-osm`.
./charly.yml:584:            charly service stop versa marimo
./charly.yml:585:            charly cmd versa "cp /data/workspace/notebooks/osm-monaco-viz.py \
./charly.yml:587:            charly service start versa marimo
./charly.yml:595:            - `/charly-versa:versa` — the image that composes this layer
./charly.yml:596:            - `/charly-versa:marimo-mcp` — the MCP server tool catalog
./charly.yml:597:            - `/charly-versa:notebook-osm` — the canonical notebook using this env
./charly.yml:605:        family: versa
./charly.yml:606:        owner: charly-versa
./charly.yml:614:            `--mcp` flag (see `/charly-versa:marimo-layer` service spec). It serves
./charly.yml:667:               charly cmd versa "/home/user/.pixi/envs/default/bin/marimo \
./charly.yml:682:            charly check live versa --filter mcp   # runs the baked mcp: ping / list-tools steps (mcp_name: marimo)
./charly.yml:715:            - `/charly-versa:marimo-layer` — layer that runs the server
./charly.yml:716:            - `/charly-versa:airflow-layer` — Airflow service in the same pod; it has no MCP wrapper
./charly.yml:717:            - `/charly-versa:notebook-osm` — example notebook diagnosed via this MCP
./charly.yml:724:        family: versa
./charly.yml:725:        owner: charly-versa
./charly.yml:733:            in the versa image. Martin (Rust) is the long-running tile server
./charly.yml:842:            `/charly-versa:notebook-osm` cell 10 uses `mo.iframe` with embedded
./charly.yml:856:            `/charly-versa:maputnik-layer` is a visual editor for these styles —
./charly.yml:858:            `/charly-versa:pmtiles-viewer` is a sibling SPA on port 28001 that
./charly.yml:898:            - `/charly-versa:versa` — image composing this layer
./charly.yml:899:            - `/charly-versa:notebook-osm` — DAG that produces pmtiles + MapLibre cell consuming them
./charly.yml:900:            - `/charly-versa:maputnik-layer` — visual style editor for the same vector tiles
./charly.yml:906:        family: versa
./charly.yml:907:        owner: charly-versa
./charly.yml:909:            Sway-browser-vnc instance wired to versa/ecovoyage tailnet URLs — chrome-devtools-mcp + CDP for debugging the generated MapLibre + folium maps.
./charly.yml:911:            # sway-browser-ecovoyage — chrome-devtools-mcp debug bed for versa/ecovoyage maps
./charly.yml:915:            rendered by the `versa/ecovoyage` notebook (`osm-monaco-viz.py`).
./charly.yml:923:            The `versa/ecovoyage` notebook generates 5 client-rendered maps
./charly.yml:924:            (4 MapLibre via `martin`, 1 shortbread via `versatiles`, 1 folium).
./charly.yml:932:            versa/ecovoyage's host ports don't resolve. Tailnet URLs solve
./charly.yml:947:            versa/ecovoyage notebook embeds in its MapLibre HTML.
./charly.yml:959:            ### Outbound — versa/ecovoyage tailnet URLs the browser will load
./charly.yml:1085:            - `/charly-versa:notebook-osm` — the marimo notebook whose 5 maps this skill exists to debug
./charly.yml:1086:            - `/charly-versa:versa` — the image hosting the maps (versa/ecovoyage = Pattern A instance)
./README.md:1:# pod-charly-versa
./README.md:3:The `pod-charly-versa` candy of the [opencharly/charly](https://github.com/opencharly/charly)
```

**4. The generation closure already carries these skills from their new owners —
the R10 proof for this deletion.** `opencharly/marketplace#325` (MERGED) ran the
generator over a closure with the nine aggregator sources REMOVED and their moved
skills deleted (git `insteadOf` to locally-patched copies) — the exact
post-deletion state — and all 37 wave skills still emitted from the distro-* pins:

```
$ charly marketplace generate --root proj-closure --out closure-out3
EXIT=0
wave skills present in closure-WITHOUT-aggregators: 37/37
missing: []
content-differs: []
```

This is the regeneration check that would FAIL (skills missing) if the distro-*
pins did not supply them.

**5. Schema-stamp bump** — `version:` is set to `2026.249.2125`, the schema
revision `charly migrate` writes for this toolchain (not the day of this PR):

```
$ charly version
2026.256.1316
```

## Wave / landing order

1. Producer PRs — MERGED + tagged.
2. `opencharly/marketplace#325` — pins the owner repos (MERGED).
3. **this PR** — activation (the deleted copies are the only remaining duplicates).

## Rulebook compliance

- **R0** — `/charly-internals:skills` + `/charly-internals:marketplace` loaded.
- **R1** — the deletion is the counterpart of a completed relocation; no divergence.
- **R2** — one coherent wave; this is its activation step.
- **R3** — one owner per skill after this landing.
- **R4** — no sleeps/retries/workarounds; `charly box validate` is the gate.
- **R5** — deprecated copies deleted (not shimmed) in the same wave; the
  repo-wide sweep above is the self-test.
- **R6** — dedicated checkout, PR-only, no force-push.
- **R7** — `charly box validate` (with negative control) ran on this branch;
  successor liveness + the repo-wide sweep ran (real output above).
- **R8/R9** — no labels/plans/schemas; no binary built here.
- **R10** — config class; the marketplace closure generation run in #325 proves
  the skills persist from their new owners (proof 4).

## Change classification

Config (skill entity deletion) → `analysed on a live system`. Proof: owners carry every deleted entity (pasted output); validate clean + negative control; repo-wide sweep reconciled; closure generation run proves the corpus is unaffected.

*Assisted-by: opencode ollama-cloud/deepseek-v4.1-flash (analysed on a live system)*